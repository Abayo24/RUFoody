<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RUFoody" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorOnPrimary">@color/purple</item>
        <!-- PrimaryVariant brand color. -->
        <item name="colorPrimaryVariant">@color/Black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/Black</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/Black</item>
        <!-- Customize your theme here. -->
    </style>
</resources>